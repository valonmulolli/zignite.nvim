# Install Locally

To install the plugin locally, you can use the following command:

```bash
nvim --headless -c "lua require('lazy').add({spec = 'file:///home/v470n/Dev/work/zignite.nvim'})" -c "Lazy install" -c "q"
```